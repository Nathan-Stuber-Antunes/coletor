{
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^18.2.1",
    "puppeteer-core": "^18.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./config/server.js"
  }
}
